---
title: "Exercices Semaine 5"
format:
  html:
    html-math-method: mathjax
    include-after-body: include/webex_2.js
    css: include/webex_2.css
toc: false
editor: source
---


```{r, echo = FALSE, results='asis'}
knitr::opts_chunk$set(echo = FALSE)
source("include/q_a_env.R")
```


<!-- JavaScript for legacy hint/explanation buttons -->
<script type="text/javascript">
function toggleAlertLegacy(alertId, message, type) {
  var alertBox = document.getElementById(alertId);
  if (!alertBox) return;

  var alertsContainer = alertBox.closest('.question-alerts');
  if (alertsContainer) {
    var allAlerts = alertsContainer.querySelectorAll('.alert');
    allAlerts.forEach(function(alert) {
      if (alert.id !== alertId) {
        alert.style.display = "none";
      }
    });
  }

  var alertMessage = alertBox.querySelector(".alertMessage");
  var alertType = alertBox.querySelector(".alertType");

  if (alertBox.style.display === "block") {
    alertBox.style.display = "none";
  } else {
    alertMessage.innerHTML = message;
    alertType.innerHTML = (type === 'hint') ? '&#10067; Indice' : '&#128161; Explication';

    var isDark = document.documentElement.getAttribute('data-theme') === 'dark';
    alertBox.style.backgroundColor = (type === 'hint')
      ? (isDark ? '#3a3520' : '#FFEDA3')
      : (isDark ? '#1a3a2a' : '#C0EDC2');
    alertBox.style.borderColor = (type === 'hint')
      ? (isDark ? '#d69e2e' : '#E5B800')
      : (isDark ? '#68d391' : '#388E3C');
    alertBox.style.color = isDark ? '#e2e8f0' : '#1a202c';
    alertBox.style.display = "block";

    MathJax.typesetPromise([alertMessage]);
  }
}
</script>

<!-- CSS for AI Help button and alerts container -->
<style>
.custom-button.ai-help {
  background: #E8E0F0;
  border: 2px solid #5a3d8a;
}

.custom-button.ai-help:hover {
  background: linear-gradient(135deg, #5a67d8 0%, #9f7aea 100%);
  border-color: transparent;
  color: white;
  transform: scale(1.05);
}

[data-theme="dark"] .custom-button.ai-help {
  background: #2d2440;
  border-color: #7c6aad;
  color: #e2e8f0;
}

[data-theme="dark"] .custom-button.ai-help:hover {
  background: linear-gradient(135deg, #5a67d8 0%, #9f7aea 100%);
  border-color: transparent;
  color: white;
}

.question-alerts {
  margin-top: 8px;
}

.question-alerts .alert {
  margin-bottom: 8px;
}
</style>

<!-- MathJax for Rendering Math -->
<script type="text/javascript" async
  src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/3.2.2/es5/tex-mml-chtml.js">
</script>


---

## Question 1

Pour la distribution standard normale \\(\\mathcal{N}(0,1)\\), combien vaut \\(\\Phi(0)\\)?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0\\)",
       feedback = "Faux"),
  list(value = "\\(1\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.50\\)",
       feedback = "Vrai"),
  list(value = "\\(-1\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q1-hint', 'Consultez la table de la fonction cumulative de densité de la distribution standard normale.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q1-expl', 'Pour la distribution standard normale \\(\\mathcal{N}(0,1)\\), \\(\\Phi(0) = 0.5\\). La moyenne et à la médiane de la distribution correspondent à \\(0\\) et ainsi, la probabilité qu\'une variable aléatoire distribuée selon la distribution standard normale soit plus petite ou égale à \\(0\\) est de \\(0.5\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q1-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q1-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


## Question 2

Soit  \\(X \\sim \\mathcal{N}(0,1)\\), à quoi correspond \\(P(X>0)\\)?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.50\\)",
       feedback = "Vrai"),
  list(value = "\\(1\\)",
       feedback = "Faux"),
  list(value = "\\(-1\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q2-hint', 'Consultez la table de la fonction cumulative de densité de la distribution standard normale et calculez le complément.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q2-expl', 'Soit \\(X \\sim \\mathcal{N}(0,1)\\), \\(P(X>0) = 1- \\Phi(0) = 1- 0.5 = 0.5\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q2-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q2-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


## Question 3

Pour la distribution standard normale \\(\\mathcal{N}(0,1)\\), combien vaut \\(\\Phi^{-1}(0.50)\\)?



```{r, results='asis'}
opts <- list(
  answer = list(value = "\\(0\\)",
       feedback = "Vrai"),
  list(value = "\\(0.50\\)",
       feedback = "Faux"),
  list(value = "\\(1\\)",
       feedback = "Faux"),
  list(value = "\\(-1\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q3-hint', 'Consultez la table de la fonction cumulative de densité de la distribution standard normale trouver la valeur \\(x \\text{ tel que } P(X < x) = 0.5\\)', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q3-expl', '\\( \\Phi^{-1}(0.50)\\) correspond à la valeur pour laquelle la fonction de répartition de la loi normale standard atteint \\(0.50\\). Ainsi, \\( \\Phi^{-1}(0.50) = 0\\)', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q3-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q3-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


## Question 4

Pour la distribution standard normale \\(\\mathcal{N}(0,1)\\), combien vaut \\(\\Phi^{-1}(0.90)\\)? Les valeurs sont arrondies au dixième le plus proche.



```{r, results='asis'}
opts <- list(
  list(value = "\\(1\\)",
       feedback = "Faux"),
  list(value = "\\(-0.50\\)",
       feedback = "Faux"),
  list(value = "\\(-1.28\\)",
       feedback = "Faux"),
  answer = list(value = "\\(1.28\\)",
       feedback = "Vrai")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q4-hint', 'Consultez la table de la fonction cumulative de densité de la distribution standard normale trouver la valeur \\(x \\text{ tel que } P(X < x) = 0.9\\)', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q4-expl', '\\( \\Phi^{-1}(0.90)\\) correspond à la valeur pour laquelle la fonction de répartition de la loi normale standard atteint \\(0.90\\). Ainsi, \\( \\Phi^{-1}(0.90) \\approx 1.28\\)', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q4-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q4-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


## Question 5

Soit  \\(X \\sim \\mathcal{N}(0,1)\\), à quoi correspond \\(P(X>1)\\)? Les probabilités sont arrondies au dixième le plus proche.

```{r, results='asis'}
opts <- list(
  answer = list(value = "\\(0.16\\)",
       feedback = "Vrai"),
  list(value = "\\(0.22\\)",
       feedback = "Faux"),
  list(value = "\\(0.84\\)",
       feedback = "Faux"),
  list(value = "\\(0.75\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q5-hint', 'Consultez la table de la fonction cumulative de densité de la distribution standard normale et calculez le complément.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q5-expl', 'Soit \\(X \\sim \\mathcal{N}(0,1)\\), \\(P(X>1) = 1- \\Phi(1) = 1- 0.8413447 = 0.1586553 \\approx 0.16\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q5-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q5-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


---

**Analyse des écarts dans la production d'une usine**

Une usine de fabrication produit des pièces mécaniques dont la dimension doit être exactement de \\(0\\) mm par rapport à une référence. Cependant, des variations autour de cette valeur existent en raison du processus de fabrication. L'écart par rapport à la référence suit une loi normale standard \\(\\mathcal{N}(0,1)\\) (espérance de \\(0\\) et écart-type de \\(1\\)), où les valeurs négatives indiquent des pièces plus petites que prévu, et les valeurs positives indiquent des pièces plus grandes.

## Question 6

Une pièce est considérée comme trop grande si son écart dépasse \\(1.4\\) mm. Quelle est la probabilité qu'une pièce sélectionnée au hasard soit trop grande ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0.9192\\)",
       feedback = "Faux"),
  list(value = "\\(0.4032\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.0808\\)",
       feedback = "Vrai"),
  list(value = "\\(0.5000\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q6-hint', 'Utilisez la table de la distribution cumulative de la loi normale standard et pensez à calculer la complémentaire.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q6-expl', 'D&apos;après la table de la loi normale standard, \\(P(Z > 1.4) = 1 - \\Phi(1.4) = 1 - 0.9192 = 0.0808\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q6-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q6-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>



## Question 7

Une pièce est considérée comme trop petite si son écart est inférieur à -\\(1.1\\) mm. Quelle est la probabilité qu'une pièce sélectionnée au hasard soit trop petite ?

```{r, results='asis'}
opts <- list(
  answer = list(value = "\\(0.1357\\)",
       feedback = "Vrai"),
  list(value = "\\(0.5000\\)",
       feedback = "Faux"),
  list(value = "\\(0.8849\\)",
       feedback = "Faux"),
  list(value = "\\(0.2119\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q7-hint', 'Utilisez la table de la distribution cumulative de la loi normale standard.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q7-expl', 'On utilise la table de la loi normale standard : \\(P(Z < -1.1) = \\Phi(-1.1) = 0.1357\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q7-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q7-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>



## Question 8

Les pièces sont considérées comme conformes si leur écart est compris entre \\(-0.9\\) mm et \\(1.2\\) mm. Quelle est la probabilité qu'une pièce soit conforme ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0.5000\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.7008\\)",
       feedback = "Vrai"),
  list(value = "\\(0.2876\\)",
       feedback = "Faux"),
  list(value = "\\(0.8413\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q8-hint', 'Utilisez la table de la distribution cumulative de la loi normale standard et soustrayez deux probabilités.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q8-expl', 'On utilise \\(P(a < Z < b) = \\Phi(b) - \\Phi(a)\\) avec \\(a = -0.9\\) et \\(b = 1.2\\). On trouve \\(P(-0.9 < Z < 1.2) = 0.8849 - 0.1841 = 0.7008\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q8-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q8-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


## Question 9

Dans un pays, la durée moyenne d'un mandat politique est de \\(5.2\\) années avec une variance de \\(1.21\\) années carrées et suit une distribution normale. Quelle est la probabilité qu'un mandat dure moins de \\(4\\) années ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0.2319\\)",
       feedback = "Faux"),
  list(value = "\\(0.3085\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.1379\\)",
       feedback = "Vrai"),
  list(value = "\\(0.8413\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q9-hint', 'Standardisez en calculant \\(Z = (X - \\mu) / \\sigma\\), puis utilisez la table de la distribution normale.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q9-expl', 'On calcule \\(Z = (4 - 5.2) / \\sqrt{1.21} = -1.09\\). En utilisant la table, \\(P(Z < -1.09) = 0.1379\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q9-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q9-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


## Question 10

Le taux de croissance annuel du PIB d'un pays suit une distribution normale avec une moyenne de \\(2.5\\%\\) et un écart-type de \\(1.2\\%\\). Quelle est la probabilité que la croissance soit comprise entre \\(1\\%\\) et \\(4\\%\\) ?

```{r, results='asis'}
opts <- list(
  answer = list(value = "\\(0.7888\\)",
       feedback = "Vrai"),
  list(value = "\\(0.5000\\)",
       feedback = "Faux"),
  list(value = "\\(0.2319\\)",
       feedback = "Faux"),
  list(value = "\\(0.8413\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q10-hint', 'Standardisez les deux valeurs, puis utilisez la table et appliquez \\( P(a < Z < b) = \\Phi(b) - \\Phi(a) \\).', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q10-expl', 'On calcule \\(Z_1 = (1 - 2.5) / 1.2 = -1.25\\) et \\(Z_2 = (4 - 2.5) / 1.2 = 1.25\\). Avec la table, \\(P(-1.25 < Z < 1.25) = 0.8944 - 0.1056 = 0.7888\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q10-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q10-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>

## Question 11

Le score d'un test d'admission suit une loi normale de moyenne \\(70\\) points et d'écart-type \\(10\\) points. L'admission est garantie pour les étudiants dépassant le \\(80\\)e percentile. Quel est le score minimum requis pour être admis ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(73.8\\)",
       feedback = "Faux"),
  answer = list(value = "\\(78.4\\)",
       feedback = "Vrai"),
  list(value = "\\(85.2\\)",
       feedback = "Faux"),
  list(value = "\\(90.0\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q11-hint', 'Recherchez dans la table de la loi normale standard la valeur de \\(z\\) telle que \\(P(Z\\leq z) = 0.80\\), puis déstandardisez.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q11-expl', 'Le 80e percentile correspond à \\(P(Z\\leq z_{80}) = 0.80\\), ce qui donne \\(z_{80} \\approx 0.84\\). On déstandardise avec \\(X = \\mu + \\sigma z_{80} = 70 + 10 \\times 0.84 = 78.4\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q11-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q11-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>

## Question 12

Un institut international analyse l'Indice de Développement Humain (IDH) des pays. On suppose que l'IDH suit approximativement une loi normale de moyenne \\(0.75\\) et d'écart-type \\(0.10\\). Un pays est considéré comme "très développé" s'il se situe dans le top \\(10\\%\\) des IDH. Quel est ce seuil ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0.355\\)",
       feedback = "Faux"),
  list(value = "\\(1.154\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.878\\)",
       feedback = "Vrai"),
  list(value = "\\(0.622\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q12-hint', 'Recherchez dans la table de la loi normale standard la valeur de \\(z\\) telle que \\(P(Z\\leq z) = \\alpha\\) (quelle valeur pour \\(\\alpha\\) ?), puis déstandardisez.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q12-expl', 'Le 90e percentile correspond à \\(P(Z\\leq z_{90}) = 0.90\\), ce qui donne \\(z_{90} \\approx 1.28\\). On déstandardise avec \\(X = \\mu + \\sigma z_{90} = 0.75 + 0.10 \\times 1.28 = 0.878\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q12-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q12-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>
