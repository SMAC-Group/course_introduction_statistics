---
title: "Exercices Semaine 2"
format:
  html:
    html-math-method: mathjax
    include-after-body: include/webex_2.js
    css: include/webex_2.css
toc: false
editor: source
---


```{r, echo = FALSE, results='asis'}
knitr::opts_chunk$set(echo = FALSE)
source("include/q_a_env.R")
```


<!-- JavaScript for legacy hint/explanation buttons -->
<script type="text/javascript">
function toggleAlertLegacy(alertId, message, type) {
  var alertBox = document.getElementById(alertId);
  if (!alertBox) return;

  var alertsContainer = alertBox.closest('.question-alerts');
  if (alertsContainer) {
    var allAlerts = alertsContainer.querySelectorAll('.alert');
    allAlerts.forEach(function(alert) {
      if (alert.id !== alertId) {
        alert.style.display = "none";
      }
    });
  }

  var alertMessage = alertBox.querySelector(".alertMessage");
  var alertType = alertBox.querySelector(".alertType");

  if (alertBox.style.display === "block") {
    alertBox.style.display = "none";
  } else {
    alertMessage.innerHTML = message;
    alertType.innerHTML = (type === 'hint') ? '&#10067; Indice' : '&#128161; Explication';

    var isDark = document.documentElement.getAttribute('data-theme') === 'dark';
    alertBox.style.backgroundColor = (type === 'hint')
      ? (isDark ? '#3a3520' : '#FFEDA3')
      : (isDark ? '#1a3a2a' : '#C0EDC2');
    alertBox.style.borderColor = (type === 'hint')
      ? (isDark ? '#d69e2e' : '#E5B800')
      : (isDark ? '#68d391' : '#388E3C');
    alertBox.style.color = isDark ? '#e2e8f0' : '#1a202c';
    alertBox.style.display = "block";

    MathJax.typesetPromise([alertMessage]);
  }
}
</script>

<!-- CSS for AI Help button and alerts container -->
<style>
.custom-button.ai-help {
  background: #E8E0F0;
  border: 2px solid #5a3d8a;
}

.custom-button.ai-help:hover {
  background: linear-gradient(135deg, #5a67d8 0%, #9f7aea 100%);
  border-color: transparent;
  color: white;
  transform: scale(1.05);
}

[data-theme="dark"] .custom-button.ai-help {
  background: #2d2440;
  border-color: #7c6aad;
  color: #e2e8f0;
}

[data-theme="dark"] .custom-button.ai-help:hover {
  background: linear-gradient(135deg, #5a67d8 0%, #9f7aea 100%);
  border-color: transparent;
  color: white;
}

.question-alerts {
  margin-top: 8px;
}

.question-alerts .alert {
  margin-bottom: 8px;
}
</style>

<!-- MathJax for Rendering Math -->
<script type="text/javascript" async
  src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/3.2.2/es5/tex-mml-chtml.js">
</script>


---


## Question 1
Quelle est la règle du complément en probabilité ?


```{r, results='asis'}
opts <- list(
  list(value = "\\( P(A) = P(A^c) \\)",
       feedback = "Faux"),
  answer = list(value = "\\( P(A) = 1 - P(A^c) \\)",
       feedback = "Vrai"),
  list(value = "\\( P(A^c) = P(A) \\)",
       feedback = "Faux"),
  list(value = "\\( P(A^c) = P(A) + P(B) \\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q1-hint', 'Pensez à la probabilité totale de \\(1\\).', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q1-expl', 'La règle du complément stipule que la probabilité de l\'événement complémentaire \\(A^C\\) est \\(1-P(A)\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q1-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q1-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>



## Question 2

Si deux événements \\( A \\) et \\( B \\) sont mutuellement exclusifs, alors:

```{r, results='asis'}
opts <- list(
  list(value = "\\(P(A) + P(B) - P(A \\cap  B) > 1\\)",
       feedback = "Faux"),
  answer = list(value = "\\(P(A|B) = 0\\)",
       feedback = "Vrai"),
  list(value = "\\(P(B|A) = P(B)\\)",
       feedback = "Faux"),
  list(value = "\\(P(A \\cap B) > 0\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q2-hint', 'Deux événements sont mutuellement exclusifs s\'ils ne peuvent pas se produire en même temps. Cela signifie que leur intersection est vide.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q2-expl', 'La probabilité conditionnelle \\(P(A∣B)\\) est la probabilité que \\(A\\) se produise sachant que \\(B\\) s\'est produit. Puisque \\(A\\)  et \\(B\\) ne peuvent pas se produire ensemble, \\(P(A∣B)=P(A∣B)=0\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q2-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q2-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>



## Question 3
Un joueur lance deux dés. Quelle est la probabilité d'obtenir au moins un \\(6\\) sachant que la somme est \\(8\\) ?


```{r, results='asis'}
opts <- list(
  list(value = "\\( \\frac{1}{3} \\)",
       feedback = "Faux"),
  answer = list(value = "\\( \\frac{2}{5} \\)",
       feedback = "Vrai"),
  list(value = "\\( \\frac{2}{3} \\)",
       feedback = "Faux"),
  list(value = "\\( \\frac{1}{4}\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q3-hint', 'Quelles sont les paires dont la somme est \\(8\\)?', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q3-expl', 'Les paires de dés donnant une somme de \\(8\\) sont \\((2,6), (6,2), (3,5), (5,3), (4,4) \\). Parmi elles, \\(2\\) ont un \\(6\\), soit \\(\\frac{2}{5}\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q3-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q3-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>



## Question 4

On considère un univers où \\(100\\) personnes ont été interrogées sur leur consommation de café et de thé. \\(60\\) boivent du café, \\(40\\) boivent du thé et \\(20\\) boivent les deux. Quelle est la probabilité qu'une personne choisie au hasard boive du café ou du thé ?

```{r, results='asis'}
opts <- list(
  list(value = "\\( \\frac{70}{100} \\)",
       feedback = "Faux"),
  answer = list(value = "\\( \\frac{80}{100} \\)",
       feedback = "Vrai"),
  list(value = "\\( \\frac{60}{100} \\)",
       feedback = "Faux"),
  list(value = "\\( \\frac{90}{100} \\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q4-hint', 'Pensez à la règle de l\'union des probabilités.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q4-expl', 'Utilisation de la règle de l'union des probabilités : \\( P(C \\cup T) = P(C) + P(T) - P(C \\cap T) \\), soit \\( \\frac{60}{100} + \\frac{40}{100} - \\frac{20}{100} = \\frac{80}{100}\\)', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q4-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q4-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>



## Question 5

Considérez la série suivante:

\\(S_n = \\sum_{k=1}^{n} \\frac{ (-1)^k }{ k^2 }\\)

Quelle est la valeur de \\(S_5\\)?



```{r, results='asis'}
opts <- list(
  list(value = "\\( - \\frac{3000}{3650} \\)",
       feedback = "Faux"),
  list(value = "\\( \\frac{25}{450} \\)",
       feedback = "Faux"),
  list(value = "\\( - \\frac{125}{865} \\)",
       feedback = "Faux"),
  answer = list(value = "\\( - \\frac{3019}{3600} \\)",
       feedback = "Vrai")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q5-hint', 'Écrivez chacun des termes.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q5-expl', '\\(S_5 = -\\frac{1}{1^2} + \\frac{1}{2^2} - \\frac{1}{ 3^2 } + \\frac{1}{4^2} - \\frac{1}{25} = -\\frac{3019}{3600} \\)', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q5-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q5-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


---


**Jeu avec probabilités**

Un jeu de société propose une épreuve où un joueur doit tirer **au hasard** une boule d'un sac.

Le sac contient **5 types de boules**, chacune ayant une valeur de points différente et une probabilité spécifique d'être tirée :

| Type de boule | Valeur (points) | Probabilité |
|--------------|-----------------|-------------|
| \\(A\\) | \\(2\\) points | \\(0.1\\) |
| \\(B\\) | \\(4\\) points | \\(0.2\\) |
| \\(C\\) | \\(6\\) points | \\(0.3\\) |
| \\(D\\) | \\(8\\) points | \\(0.25\\) |
| \\(E\\) | \\(10\\) points | \\(0.15\\) |


## Question 6

Quelle est l'espérance du nombre de points obtenus en tirant une boule au hasard ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(5.3\\)",
       feedback = "Faux"),
  list(value = "\\(6.2\\)",
       feedback = "Faux"),
  list(value = "\\(6.5\\)",
       feedback = "Faux"),
  answer = list(value = "\\(6.3\\)",
       feedback = "Vrai")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q6-hint', 'Utilisez la formule de l'espérance : \\(E(X) = \\sum_{\\text{tout }k} k P(X=k)\\) où \\(k\\) correspond à un type de boule.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q6-expl', 'L'espérance se calcule par : \\(E(X) = (2 \\times 0.1) + (4 \\times 0.2) + (6 \\times 0.3) + (8 \\times 0.25) + (10 \\times 0.15) = 6.3\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q6-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q6-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


## Question 7

Quelle est la probabilité d'obtenir une boule valant \\(6\\) points ou plus ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0.3\\)",
       feedback = "Faux"),
  list(value = "\\(0.6\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.7\\)",
       feedback = "Vrai"),
  list(value = "\\(1.0\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q7-hint', 'Utilisez la formule fondamentale de la probabilité.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q7-expl', 'On cherche \\(P(X\\geq6)=P(6)+P(8)+P(10)=0.3 + 0.25 + 0.15 = 0.7\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q7-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q7-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>

---

**Probabilités et organisations internationales**

Une étude a été réalisée sur la participation des pays membres à différentes organisations internationales. On considère qu'un pays peut appartenir à plusieurs organisations à la fois. Un échantillon de \\(200\\) pays a donné les résultats suivants (données fictives) :

- **\\(120\\) pays** sont membres de l'**ONU**.
- **\\(90\\) pays** sont membres de l'**OMC**.
- **\\(60\\) pays** sont membres de l'**Union Africaine**.
- **\\(50\\) pays** sont membres à la fois de l'ONU et de l'OMC.
- **\\(30\\) pays** sont membres à la fois de l'ONU et de l'Union Africaine.
- **\\(20\\) pays** sont membres à la fois de l'OMC et de l'Union Africaine.
- **\\(10\\) pays** sont membres des trois organisations.

## Question 8

Quelle est la probabilité qu'un pays choisi au hasard soit membre de l'ONU ou de l'OMC ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0.75\\)",
       feedback = "Faux"),
  list(value = "\\(0.85\\)",
       feedback = "Faux"),
  list(value = "\\(1.10\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.80\\)",
       feedback = "Vrai")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q8-hint', 'Utilisez la formule de l'union des probabilités.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q8-expl', 'La probabilité qu'un pays soit membre de l'ONU ou de l'OMC est donnée par : \\( P(A \\cup B) = P(A) + P(B) - P(A \\cap B) \\). En remplaçant : \\( \\frac{120}{200} + \\frac{90}{200} - \\frac{50}{200} = 0.80 \\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q8-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q8-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


## Question 9

Quelle est la probabilité qu'un pays choisi au hasard soit membre de l'ONU mais pas de l'OMC ni de l'Union Africaine ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0.20\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.25\\)",
       feedback = "Vrai"),
  list(value = "\\(0.30\\)",
       feedback = "Faux"),
  list(value = "\\(0.60\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q9-hint', 'Utilisez la formule de l'union des probabilités.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q9-expl', 'Les pays uniquement membres de l'ONU sont : \\( 120 - (50 + 30 - 10) = 50 \\), soit une probabilité de \\( \\frac{50}{200} = 0.25 \\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q9-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q9-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>


## Question 10

Quelle est la probabilité qu'un pays choisi au hasard soit membre d'au moins deux organisations ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0.45\\)",
       feedback = "Faux"),
  list(value = "\\(0.55\\)",
       feedback = "Faux"),
  list(value = "\\(0.50\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.40\\)",
       feedback = "Vrai")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q10-hint', 'Additionnez les pays qui sont dans plusieurs organisations. Évitez de compter plusieurs fois le même pays.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q10-expl', 'Les pays qui sont membres d'au moins deux organisations sont ceux qui sont dans les intersections : \\( 50 + 30 + 20 - 2 \\times 10 = 80 \\), soit une probabilité de \\(\\frac{80}{200} = 0.40 \\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q10-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q10-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>



## Question 11

Un pays est sélectionné au hasard. Sachant qu'il est membre de l'ONU, quelle est la probabilité qu'il soit également membre de l'Union Africaine ?

```{r, results='asis'}
opts <- list(
  list(value = "\\(0.50\\)",
       feedback = "Faux"),
  list(value = "\\(0.33\\)",
       feedback = "Faux"),
  answer = list(value = "\\(0.25\\)",
       feedback = "Vrai"),
  list(value = "\\(0.55\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q11-hint', 'Utilisez la définition de la probabilité conditionnelle : \\( P(A|B) = \\frac{P(A \\cap B)}{P(B)} \\).', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q11-expl', 'La probabilité conditionnelle est donnée par : \\( P(UA | ONU) = \\frac{P(UA \\cap ONU)}{P(ONU)} = \\frac{30}{120} = 0.25 \\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q11-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q11-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>



## Question 12

L'appartenance à l'ONU et à l'Union Africaine sont-elles des événements indépendants ?

```{r, results='asis'}
opts <- list(
  list(value = "Oui, car \\( P(A \\cap B) = P(A) P(B) \\)",
       feedback = "Faux"),
  answer = list(value = "Non, car \\( P(A \\cap B) \\neq P(A) P(B) \\)",
       feedback = "Vrai"),
  list(value = "Oui, car les effectifs sont proportionnels",
       feedback = "Faux"),
  list(value = "Non, car un pays ne peut appartenir qu'à une seule organisation",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q12-hint', 'Comparez \\( P(A \\cap B) \\) avec \\( P(A) P(B) \\) pour vérifier l\'indépendance.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q12-expl', 'Les événements sont indépendants si \\( P(A \\cap B) = P(A) P(B) \\). Ici, \\( P(ONU) = \\frac{120}{200} \\), \\( P(UA) = \\frac{60}{200} \\) et \\( P(ONU \\cap UA) =  \\frac{30}{200} = 0.15 \\). Or, \\( P(ONU) P(UA) = \\frac{120}{200} \\times \\frac{60}{200} = 0.18 \\neq 0.15 \\), donc les événements ne sont pas indépendants.', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q12-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q12-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>



---


**Participation aux votations fédérales**

## Question 13

Lors des votations fédérales, la participation électorale varie d'un canton à l'autre. Supposons que la probabilité qu'une personne du canton de Genève participe à une votation soit de \\(55\\%\\), tandis que dans le canton de Zurich, cette probabilité est de \\(50\\%\\) (données fictives). On sélectionne une personne au hasard dans chacun de ces deux cantons. En supposant que les décisions de participation des personnes des deux cantons sont indépendantes, quelle est la probabilité que les deux personnes participent à la votation ?

```{r, results='asis'}
opts <- list(
  answer = list(value = "\\(0.275\\)",
       feedback = "Vrai"),
  list(value = "\\(0.90\\)",
       feedback = "Faux"),
  list(value = "\\(0.50\\)",
       feedback = "Faux"),
  list(value = "\\(1.10\\)",
       feedback = "Faux")
)
render_q_a_env(opts)
```



<div class="question-buttons">
<button class="custom-button hint" onclick="toggleAlertLegacy('q13-hint', 'Utilisez la règle de multiplication pour des événements indépendants.', 'hint')">&#10067; Indice</button>
<button class="custom-button explanation" onclick="toggleAlertLegacy('q13-expl', 'Pour des événements indépendants, la probabilité que les deux personnes électrices participent est le produit des probabilités individuelles : \\(0.55 \\times 0.50 = 0.275\\).', 'explanation')">&#128161; Explication</button>
</div>
<div class="question-alerts">
<div class="alert alert-info" id="q13-hint" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
<div class="alert alert-info" id="q13-expl" style="display: none;">
  <strong class="alertType"></strong>: <span class="alertMessage"></span>
</div>
</div>
